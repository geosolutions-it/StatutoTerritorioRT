{
   "operationName" : "DeleteDelega",

   "variables" : {
         "token" : "{codice}"
   },

   "query" : "mutation DeleteDelega($token: String!) {
        deleteDelega(token: $token) { errors}
   }"
}
